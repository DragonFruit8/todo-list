
function TodoListItem({todoItem = []}) {
    // const todoItem = props.todoItem;
    return (
        <ul>
            {todoItem.map((item) => {
                return
            })}
        </ul>
    );
};

export default TodoListItem;